# -*- Python -*-

import os

# These tests require the CrossTU functionality
if not config.root.clang_has_crosstu:
    config.unsupported = True

# Performance tests may take longer to run
config.timeout = 300